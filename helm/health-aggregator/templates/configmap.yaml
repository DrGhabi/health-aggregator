apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "health-aggregator.fullname" . }}-cm
  labels:
    {{- include "health-aggregator.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ .Values.config | toYaml | nindent 4 }}
